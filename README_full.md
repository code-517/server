# UNION ARENA 卡牌牌組管理系統

> 一個專為 UNION ARENA 集換式卡牌遊戲設計的線上牌組管理平台，提供卡片瀏覽、組牌工具、牌組分享與社群互動功能。

## 📋 目錄

- [專案簡介](#專案簡介)
- [核心功能](#核心功能)
- [技術架構](#技術架構)
- [安全性設計](#安全性設計)
- [安裝與部署](#安裝與部署)
- [專案結構](#專案結構)
- [API 設計](#api-設計)
- [資料庫設計](#資料庫設計)
- [效能優化](#效能優化)

---

## 🎯 專案簡介

本專案是一個全端 Web 應用程式，為 UNION ARENA 卡牌遊戲玩家提供：
- **完整的卡片資料庫**：收錄 50+ 系列、數千張卡片的詳細資料
- **線上組牌工具**：拖拽式操作、即時價格計算、自動合併相同卡片
- **牌組分享平台**：支援匿名分享、留言討論、篩選搜尋
- **響應式設計**：完整支援桌面與移動設備

### 開發動機
作為 TCG 玩家，發現市面上缺乏針對 UNION ARENA 的專業組牌工具，因此開發此平台來解決：
1. 卡片資訊分散、查詢不便
2. 缺乏線上組牌與價格計算工具
3. 牌組分享與討論平台的需求

---

## ✨ 核心功能

### 1. 卡片瀏覽系統
- 📚 **多系列支援**：鬼滅之刃、咒術迴戰、我的英雄學院等 50+ 系列
- 🔍 **詳細資料展示**：卡片效果、觸發條件、稀有度、市場價格
- 🖼️ **圖片輪播**：高品質卡面圖片展示
- 🏷️ **智能標籤**：自動標示 AP 卡、特殊能力卡等

### 2. 組牌工具
- ➕ **一鍵加卡**：點擊即可將卡片加入牌組
- 🔢 **數量管理**：自動合併相同卡片、顯示數量
- 💰 **價格計算**：即時計算牌組總價
- 💾 **臨時儲存**：使用 Cookie 保存編輯中的牌組
- 🎲 **隨機牌組名稱**：自動生成唯一的 4 字元牌組 ID

### 3. 牌組分享
- 🌐 **公開分享**：生成獨立的牌組頁面
- 🔎 **篩選搜尋**：依系列、名稱快速查找
- 💬 **留言系統**：支援匿名留言討論（限 20 則/牌組）
- 📊 **統計資訊**：顯示留言數、最後活動時間
- 🗑️ **自動清理**：30 天未活動的牌組自動刪除

### 4. 系統功能
- 🎰 **抽獎系統**：一番賞抽獎模擬器
- 📖 **功能說明**：輪播圖展示使用教學
- 🔒 **匿名用戶系統**：基於 UUID 的用戶識別

---

## 🏗️ 技術架構

### 後端技術
```
Node.js 18+
├── Express 5.x         # Web 框架
├── MongoDB Atlas       # 雲端資料庫
├── Mongoose 8.x        # ODM
├── EJS                 # 伺服器端模板引擎
└── dotenv              # 環境變數管理
```

### 安全性中間件
```javascript
helmet              // 設定 HTTP 安全標頭
csurf               // CSRF 跨站請求偽造防護
express-rate-limit  // API 請求頻率限制
cookie-parser       // Cookie 解析與管理
```

### 前端技術
- **原生 JavaScript**：無框架依賴，輕量高效
- **EJS 模板引擎**：動態內容渲染
- **CSS3**：響應式佈局、動畫效果
- **Intersection Observer API**：圖片懶加載

---

## 🔒 安全性設計

### 1. CSRF 防護
```javascript
app.use(csrf({ cookie: true }));
// 所有表單請求需附帶 CSRF token
```

### 2. 請求頻率限制
- **全域限制**：每分鐘最多 80 次請求
- **留言限制**：每分鐘最多 5 則留言
- 防止暴力攻擊與惡意灌水

### 3. XSS 防護
```javascript
// 留言內容轉義處理
const escapeHtml = (unsafe) => {
  return unsafe
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    // ...
};
```

### 4. Content Security Policy
```javascript
helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'"],
      // 嚴格限制資源來源
    }
  }
})
```

### 5. 資料驗證
- 請求大小限制（1MB）
- 留言長度驗證
- 牌組數量上限控制

---

## 🚀 安裝與部署

### 環境需求
- Node.js 18 或以上版本
- MongoDB Atlas 帳號（或本地 MongoDB）
- npm 或 yarn 套件管理器

### 安裝步驟

1. **克隆專案**
```bash
git clone <repository-url>
cd ex
```

2. **安裝依賴**
```bash
npm install
```

3. **環境變數設定**

建立 `.env` 檔案：
```env
# MongoDB 連接字串
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# 伺服器埠號
PORT=3000
```

4. **啟動應用程式**
```bash
node pa.js
```

5. **訪問網站**
```
http://localhost:3000
```

### 部署建議
- **雲端平台**：Heroku, Railway, Render
- **環境變數**：使用平台提供的環境變數管理功能
- **資料庫**：建議使用 MongoDB Atlas（免費方案已足夠）
- **靜態資源**：考慮使用 CDN 加速圖片載入

---

## 📁 專案結構

```
ex/
├── pa.js                    # 主應用程式檔案
├── package.json             # 專案配置與依賴
├── .env                     # 環境變數（不納入版控）
│
├── views/                   # EJS 模板檔案
│   ├── series.ejs          # 系列選擇頁面
│   ├── card-details.ejs    # 卡片詳細資訊
│   ├── deck-details.ejs    # 牌組詳細頁面
│   ├── decks.ejs           # 牌組列表
│   ├── use.ejs             # 使用說明
│   └── index.ejs           # 抽獎頁面
│
├── scripts/                 # 前端 JavaScript
│   ├── card-details.js     # 卡片頁面邏輯
│   ├── deck-details.js     # 牌組頁面邏輯
│   └── use.js              # 輪播圖功能
│
├── public/                  # 靜態資源
│   └── styles/             # CSS 樣式
│
├── images/                  # 卡片圖片資源
│   ├── series/             # 系列封面
│   ├── slider/             # 輪播圖片
│   └── [各系列資料夾]/     # 卡片圖片
│
├── card/                    # 卡片資料（JSON）
│   ├── 鬼滅之刃.json
│   ├── 咒術迴戰.json
│   └── ...
│
└── decks/                   # 牌組資料（已遷移至 MongoDB）
```

---

## 🔌 API 設計

### 卡片相關
| 端點 | 方法 | 說明 |
|------|------|------|
| `/series` | GET | 取得所有系列列表 |
| `/cards/:series` | GET | 取得特定系列的所有卡片 |

### 牌組管理
| 端點 | 方法 | 說明 |
|------|------|------|
| `/deck/add` | POST | 新增卡片到牌組 |
| `/deck/remove` | POST | 從牌組移除卡片 |
| `/deck/save` | POST | 儲存牌組（生成分享連結） |
| `/deck/clear` | POST | 清空臨時牌組 |
| `/deck/current` | GET | 取得當前用戶的牌組 |

### 牌組瀏覽
| 端點 | 方法 | 說明 |
|------|------|------|
| `/decks` | GET | 取得所有公開牌組（支援篩選） |
| `/decks/:deckName` | GET | 取得特定牌組詳細資料 |

### 社群互動
| 端點 | 方法 | 說明 |
|------|------|------|
| `/shared-decks/:deckName/comment` | POST | 發表牌組留言 |

### 請求範例
```javascript
// 新增卡片到牌組
POST /deck/add
Content-Type: application/json

{
  "card": {
    "card_number": "UA01BT/BLC-1-001",
    "rare": "SR",
    "image_url": "/images/...",
    "card_name": "アスタ",
    "series": "黑色五葉草",
    "details": { ... },
    "money": "100 円",
    "number": 1
  }
}
```

---

## 💾 資料庫設計

### Card Schema（動態集合）
```javascript
{
  series: String,         // 系列名稱
  card_name: String,      // 卡片名稱
  trcard_name: String,    // 翻譯後名稱
  card_number: String,    // 卡片編號
  rare: String,           // 稀有度
  details: Object,        // 卡片詳細資料
  money: String,          // 市場價格
  image_url: String       // 圖片路徑
}
```

### Deck Schema
```javascript
{
  deckName: String,       // 牌組名稱（4字元隨機 ID）
  deckIdea: String,       // 牌組理念/說明
  cards: [CardObject],    // 卡片陣列
  owner: String,          // 匿名用戶 ID（UUID）
  createdAt: Date,        // 建立時間
  lastAccessed: Date      // 最後訪問時間
}
```

### Comment Schema
```javascript
{
  deckName: String,       // 牌組名稱
  anonymousId: String,    // 留言者 ID
  comment: String,        // 留言內容（已轉義）
  timestamp: Date         // 留言時間
}
```

### 索引優化
```javascript
// 建議建立的索引
Deck.index({ deckName: 1 });
Deck.index({ owner: 1 });
Deck.index({ lastAccessed: 1 });
Comment.index({ deckName: 1, timestamp: -1 });
```

---

## ⚡ 效能優化

### 1. 圖片懶加載
```javascript
// 使用 Intersection Observer API
const imgObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;
      imgObserver.unobserve(img);
    }
  });
});
```

### 2. 資料庫查詢優化
- 使用 `.lean()` 減少記憶體使用
- 建立適當的索引
- 限制查詢結果數量

### 3. 快取策略
- 靜態資源快取（圖片、CSS、JS）
- Cookie-based 用戶狀態管理

### 4. 自動清理機制
```javascript
// 每日清理 30 天未活動的牌組
setInterval(async () => {
  const oneMonthAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000);
  await Deck.deleteMany({
    lastAccessed: { $lt: oneMonthAgo },
    deckName: { $exists: true }
  });
}, 24 * 60 * 60 * 1000);
```

---

## 🎨 設計亮點

### 1. 系列切換智能清空
當用戶切換系列時，系統會自動檢查臨時牌組：
- 如果牌組包含多個系列的卡片 → 自動清空
- 如果牌組只有單一系列但與目標系列不同 → 自動清空
- 防止跨系列組牌的混亂

### 2. 卡片智能合併
```javascript
// 自動合併相同卡片
const uniqueKey = `${card.card_number}-${card.rare}`;
if (cardMap[uniqueKey]) {
  cardMap[uniqueKey].number += 1;
} else {
  cardMap[uniqueKey] = { ...card, number: 1 };
}
```

### 3. 匿名用戶系統
- 使用 UUID v4 生成唯一識別碼
- Cookie 有效期 30 天
- 無需註冊即可使用所有功能

### 4. 隨機牌組命名
```javascript
// 生成格式：[a-z]{2}[0-9]{2}
// 範例：ab12, xy89
const name = randomLetters(2) + randomNumbers(2);
```

---

## 🛠️ 開發工具

### 使用的套件
```json
{
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.14.3",
    "ejs": "^3.1.10",
    "helmet": "^8.1.0",
    "csurf": "^1.10.0",
    "express-rate-limit": "^7.5.0",
    "cookie-parser": "^1.4.7",
    "uuid": "^11.1.0",
    "dotenv": "^16.5.0"
  }
}
```

### 開發時使用的工具
- **cheerio** / **puppeteer**：爬取卡片資料
- **axios**：API 請求
- **playwright**：自動化測試（開發階段）

---

## 📈 未來規劃

- [ ] 用戶登入系統（OAuth 2.0）
- [ ] 牌組評分與點讚功能
- [ ] 卡片收藏清單
- [ ] 進階搜尋（多條件篩選）
- [ ] 匯出牌組為圖片/PDF
- [ ] PWA 支援（離線瀏覽）
- [ ] 即時對戰模擬器

---

## 📝 授權

本專案採用 **ISC License**

---

## 👨‍💻 開發者

如有任何問題或建議，歡迎聯繫。
---

## 🙏 致謝

- 卡片資料來源：UNION ARENA 官方網站
- 圖片版權歸原作者所有
- 感謝 UNION ARENA 玩家社群的支援
