<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=390, initial-scale=1.0"/>
  <title>一番賞｜抽卡</title>
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: "Noto Sans TC", sans-serif;
      background: #0f1020;
      color: #eee;
      text-align: center;
      min-height: 100vh;
      min-width: 100vw;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 390px;
      height: 844px;
      margin: auto;
      box-sizing: border-box;
      overflow: hidden;
    }
    h1 { margin-bottom: 20px; }

    .cards {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 20px;
      margin-top: 30px;
      margin-bottom: 30px;
      flex-wrap: wrap;
    }
    .ticket-wrapper {
      position: relative;
      width: 160px;
      height: 80px;
      perspective: 800px;
      user-select: none;
    }
    .ticket {
      position: relative;
      width: 100%;
      height: 100%;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 8px 16px rgba(0,0,0,0.35);
    }
    .prize {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.4rem;
      font-weight: 800;
      background: #ffd54f;
      color: #3b2f00;
      z-index: 1;
    }
    .cover {
      position: absolute;
      inset: 0;
      background: repeating-linear-gradient(
        -45deg,
        #d0d0d0,
        #d0d0d0 8px,
        #c0c0c0 8px,
        #c0c0c0 16px
      );
      color: #333;
      font-weight: bold;
      font-size: 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: grab;
      z-index: 2;
      transform-origin: left center;
      transition: transform 0.3s ease-out;
    }
    .cover.dragging { transition: none; cursor: grabbing; }
    @media (max-width: 400px) {
      body {
        width: 100vw;
        height: 100vh;
      }
      .ticket-wrapper {
        width: 45vw;
        height: 22vw;
      }
    }
    #result-modal {
      position: fixed;
      top: 0; left: 0; width: 100vw; height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 9999;
    }
    .modal-bg {
      position: absolute;
      top: 0; left: 0; width: 100vw; height: 100vh;
      background: rgba(0,0,0,0.6);
      z-index: 1;
    }
    .modal-content {
      position: relative;
      z-index: 2;
      background: #fffbe8;
      border-radius: 24px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.3);
      padding: 40px 32px;
      display: flex;
      align-items: center;
      gap: 24px;
      font-size: 2rem;
      font-weight: bold;
      flex-direction: row;
      min-width: 240px;
      min-height: 120px;
      justify-content: center;
      animation: pop 0.4s;
    }
    .firework {
      font-size: 2.5rem;
      animation: fireworks 1.2s infinite alternate;
    }
    .firework.left { animation-delay: 0s; }
    .firework.right { animation-delay: 0.6s; }
    .prize-text {
      color: #d2691e;
      font-size: 2.2rem;
      margin: 0 16px;
      font-weight: 900;
      letter-spacing: 2px;
    }
    #close-modal {
      position: absolute;
      bottom: 16px;
      right: 24px;
      font-size: 1rem;
      padding: 6px 18px;
      border-radius: 8px;
      border: none;
      background: #ffd54f;
      color: #3b2f00;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    }
    @keyframes pop {
      0% { transform: scale(0.7); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    @keyframes fireworks {
      0% { transform: scale(1) rotate(-10deg);}
      100% { transform: scale(1.2) rotate(10deg);}
    }
  </style>
</head>
  <body>
    <h1>寶寶專屬一番賞!!</h1>
    <p>選一張，撕開看看你抽到什麼！</p>

<!-- ...前面內容不變... -->
    <div class="cards">
      <% shuffled.forEach((p, i) => { %>
        <div class="ticket-wrapper" data-prize="<%= p %>">
          <div class="ticket">
            <div class="prize" data-prize="<%= p %>">🎉 <%= p %> 🎉</div>
            <div class="cover">撕開我</div>
          </div>
        </div>
      <% }) %>
    </div>

    <!-- 撕開音效 -->
    <audio id="tear-audio" src="/sounds/tear.mp3"></audio>

    <!-- 彈窗：抽獎結果 -->
    <div id="result-modal" style="display:none;">
      <div class="modal-bg"></div>
      <div class="modal-content">
        <span class="firework left">🎆</span>
        <span id="result-prize" class="prize-text"></span>
        <span class="firework right">🎆</span>
        <button id="close-modal">關閉</button>
      </div>
    </div>

    <!-- 在最底下加這行 -->
    <script src="/scripts/index.js"></script>
  </body>
</html>
